## 1. Foundation & Layout

- [x] 1.1 Update Tailwind configuration with the new "Smart Calendar" color palette and typography.
- [x] 1.2 Implement the `AppShell` component with a responsive sidebar (desktop) and bottom-bar (mobile).
- [x] 1.3 Create the `ResponsiveLayout` wrapper to handle adaptive container widths and margins.

## 2. Atomic UI Components

- [x] 2.1 Develop the `EventCard` component with support for adaptive layouts (horizontal for desktop, vertical for mobile).
- [x] 2.2 Implement the `ScoreBadge` component with color-coding based on score value.
- [x] 2.3 Create the `TimelineMarker` component for chronological date markers in the timeline view.

## 3. Core Views Implementation

- [x] 3.1 Implement the `EventDashboard` view with smart grouping (Recommended vs. Upcoming).
- [x] 3.2 Build the `TimelineView` with a vertical scrollable list of events and date markers.
- [x] 3.3 Develop the `CalendarView` with a monthly grid visualization.
- [x] 3.4 Create the `EventDetailPanel` as a right-side slide-over component.

## 4. Integration & Polish

- [x] 4.1 Connect the views to the existing event data store.
- [x] 4.2 Implement the view-switching logic (Dashboard $\leftrightarrow$ Timeline $\leftrightarrow$ Calendar).
- [x] 4.3 Add smooth transition animations between views and when opening the detail panel.
- [x] 4.4 Perform cross-device testing on mobile and desktop viewports to ensure responsiveness.
